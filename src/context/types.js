//  El archivo type funciona para saber que modalidad tenemos dentro de nustro state
// type.js va contener una lista de los que podemos ejecutar dentro de este estado

export const GET_USERS = 'GET_USERS'
//  Esto se hace para cuando tengamos que ejecutar determinda funcionalidad no tengamos que llamar a la funtion, simplemente llamamos el tipo GET_USERS

export const GET_PROFILE = 'GET_PROFILE'